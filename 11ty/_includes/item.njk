<!DOCTYPE html>
<html lang="{{ item.mainLang }}">
<head>
  {% set pageTitle = item.title %}
  {% set pageDescription = item.contextLabel[item.mainLang] + " — " + item.title %}
  {% set pageLangs = item.langs %}
  {% include "partials/head.njk" %}
</head>
<body class="{% if item.mainLang != 'en' %}main-{{ item.mainLang }} {% endif %}{% if item.place %}place-{{ item.place.slug }}{% endif %}">
  {% include "partials/lang.njk" %}

  {% for lang in item.langs %}
  <div id="{{ lang }}" class="lang-{{ lang }}" lang="{{ lang }}">
    {% if item.place %}
    {% if item.place.image %}
    <header>
      <img src="../img/{{ item.place.image }}"
           alt="{{ item.place.label[lang] }}">
    </header>
    {% endif %}

    {% set adjacentPlaces = item.place.adjacent %}
    {% set navLang = lang %}
    {% set navLangs = item.langs %}
    {% set switcherLangs = item.langs %}
    {% include "partials/navbar.njk" %}

    <p class="item-context">{{ item.contextLabel[lang] }} · {{ item.place.label[lang] }}</p>
    <p class="item-back">
      <a href="../rooms/{{ item.place.slug }}.html#{{ lang }}">{% if lang == "ru" %}← назад{% elif lang == "zh" %}← 返回{% else %}← back{% endif %}</a>
    </p>
    {% endif %}

    {% if item.content and item.content[lang] %}
    <article class="item-body prose-body">
      {{ item.content[lang] | safe }}
    </article>
    {% endif %}

    {% if item.category == "legend" %}
    {% include "items/legend.njk" %}
    {% elif item.category == "poem-reading" %}
    {% include "items/poem-reading.njk" %}
    {% else %}
    {% include "items/default.njk" %}
    {% endif %}

    {% include "partials/footer.njk" %}
  </div>
  {% endfor %}

</body>
</html>
